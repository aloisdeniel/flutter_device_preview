name: device_preview_screenshot
version: 1.0.0-alpha.1
description: A plugin for device_preview that allow to take screenshots.
homepage: https://github.com/aloisdeniel/flutter_device_preview/plugins/device_preview_screenshot

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  device_preview: 1.0.0-alpha.6
  flutter:
    sdk: flutter
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
